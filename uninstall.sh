# FIXME use symlinks to find the repo (~/.nanorc, ~/.nano-syntax) to find the repo, remove the symlinks & repo
# _(Use `uninstall.sh --hard` to remove your local repo itself, as well as the symlinks.)_
